// import { selectCustomizePageDomain } from '../selectors';

describe('selectCustomizePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false)
  })
})
