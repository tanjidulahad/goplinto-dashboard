/*
 * CustomizePage Messages
 *
 * This contains all the text for the CustomizePage container.
 */

import { defineMessages } from 'react-intl'

export const scope = 'app.containers.CustomizePage'

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CustomizePage container!',
  },
})
